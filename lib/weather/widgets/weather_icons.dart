import 'package:flutter/material.dart';

class WeatherIcons {
  static const sunnyIcon = Icon(
    Icons.sunny,
    color: Color.fromRGBO(245, 192, 19, 1)
  );

  static const partlyCloudyIcon = Icon(
    Icons.cloud_queue,
    color: Colors.white,
  );

  static const cloudyIcon = Icon(
    Icons.wb_cloudy,
    color: Colors.white,
  );

  static const foggyIcon = Icon(
    Icons.foggy,
    color: Colors.white,
  );

  static const rainIcon = Icon(
    Icons.water_drop,
    color: Color.fromARGB(255, 44, 124, 145),
  );

  static const snowIcon = Icon(
    Icons.ac_unit,
    color: Color.fromARGB(255, 97, 218, 248),
  );

  static const thunderstormIcon = Icon(
    Icons.thunderstorm,
    color: Color.fromARGB(255, 251, 255, 39),
  );
}