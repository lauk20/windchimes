import 'package:flutter/material.dart';

class WeatherIcons {
  static const sunnyIcon = Icon(
                            Icons.sunny,
                            color: Color.fromRGBO(245, 192, 19, 1)
                          );
}